<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Организационная структура</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
    <style>
        .grade-1-5 { border-left: 4px solid #ef4444; }
        .grade-6-10 { border-left: 4px solid #f97316; }
        .grade-11-15 { border-left: 4px solid #eab308; }
        .grade-16-20 { border-left: 4px solid #22c55e; }
        .grade-21-25 { border-left: 4px solid #3b82f6; }
        .grade-26-30 { border-left: 4px solid #8b5cf6; }
        
        /* Контейнер всего дерева */
.org-tree {
  display: table;
  margin: 0 auto;
  padding: 30px;
  border-spacing: 40px 0; /* расстояние между колонками */
  white-space: nowrap; /* не переносить */
}
                
       /* Один департамент */
.org-department {
  display: table-cell;   /* теперь каждый блок — колонка */
  vertical-align: top;
  text-align: center;
  position: relative;
  min-width: 350px;      /* минимальная ширина блока */
  padding: 0 10px;       /* небольшой внутренний отступ */
}
        
        /* Узел подразделения */
.org-node {
  margin: 0 auto 40px auto;
  position: relative;
  z-index: 2;
  display: inline-block;
}

      /* Контейнер для детей */
.org-children {
  display: table;       /* таблица для детей */
  margin: 20px auto 0 auto;
  border-spacing: 40px 0;
  position: relative;
}
        
        /* Вертикальная линия вниз от родителя */
.org-department.has-children > .org-node::after {
  content: '';
  position: absolute;
  bottom: -20px;
  left: 50%;
  transform: translateX(-50%);
  width: 2px;
  height: 20px;
  background-color: #9ca3af;
}
        
        /* Горизонтальная линия соединяющая детей */
    .org-children::before {
      content: '';
      position: absolute;
      top: -20px;
      left: 0;
      right: 0;
      height: 2px;
      background-color: #9ca3af;
    }

 /* Дети на одном уровне */
    .org-children > .org-department {
      margin: 0 20px;
    }
        
        /* Вертикальные линии от горизонтальной к каждому ребенку */
    .org-children > .org-department::before {
      content: '';
      position: absolute;
      top: -20px;
      left: 50%;
      transform: translateX(-50%);
      width: 2px;
      height: 20px;
      background-color: #9ca3af;
    }
        
        .table-container {
            max-height: 400px;
            overflow-y: auto;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
        }
        
        .excel-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 12px;
        }
        
        .excel-table th,
        .excel-table td {
            border: 1px solid #d1d5db;
            padding: 8px;
            text-align: left;
        }
        
        .excel-table th {
            background-color: #f3f4f6;
            font-weight: bold;
            position: sticky;
            top: 0;
        }
        
        .excel-table input,
        .excel-table select {
            width: 100%;
            border: none;
            padding: 4px;
            font-size: 12px;
        }

        @media (max-width: 1024px) {
            .org-level {
                gap: 30px;
            }
            
            .org-department {
                min-width: 250px;
                max-width: 320px;
            }
        }
        
        @media (max-width: 768px) {
            .org-tree {
                padding: 20px;
            }
            
            .org-level {
                gap: 20px;
                flex-direction: column;
                align-items: center;
            }
            
            .org-department {
                min-width: 280px;
                max-width: 350px;
            }
            
            /* Убираем горизонтальные линии на мобильных */
            .org-level::before {
                display: none !important;
            }
            
            .org-department::before {
                display: none !important;
            }
        }

        .required-field::after {
            content: ' *';
            color: #ef4444;
            font-weight: bold;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen p-6">
    <div class="max-w-7xl mx-auto">
        <div class="bg-white rounded-lg shadow-lg p-6 mb-6">
            <h1 class="text-3xl font-bold text-gray-800 mb-4">Организационная структура</h1>
            
            <div class="flex flex-wrap gap-4 mb-6">
                <button onclick="addRootDepartment()" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg font-medium transition-colors">
                    + Добавить корневое подразделение
                </button>
                <button onclick="toggleGradeView()" class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg font-medium transition-colors">
                    Группировка по грейдам
                </button>
                
                <!-- Экспорт -->
                <div class="relative">
                    <button onclick="toggleExportMenu()" class="bg-purple-600 hover:bg-purple-700 text-white px-4 py-2 rounded-lg font-medium transition-colors">
                        📤 Экспорт
                    </button>
                    <div id="exportMenu" class="hidden absolute top-full left-0 mt-1 bg-white border border-gray-300 rounded-lg shadow-lg z-20">
                        <button onclick="exportToJSON()" class="block w-full text-left px-4 py-2 hover:bg-gray-100 text-sm">JSON формат</button>
                        <button onclick="exportToExcel()" class="block w-full text-left px-4 py-2 hover:bg-gray-100 text-sm">Excel формат</button>
                    </div>
                </div>
                
                <!-- Импорт -->
                <div class="relative">
                    <button onclick="toggleImportMenu()" class="bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg font-medium transition-colors">
                        📥 Импорт
                    </button>
                    <div id="importMenu" class="hidden absolute top-full left-0 mt-1 bg-white border border-gray-300 rounded-lg shadow-lg z-20">
                        <label class="block w-full text-left px-4 py-2 hover:bg-gray-100 text-sm cursor-pointer">
                            JSON файл
                            <input type="file" id="importJSONFile" accept=".json" class="hidden" onchange="importFromJSON(event)">
                        </label>
                        <label class="block w-full text-left px-4 py-2 hover:bg-gray-100 text-sm cursor-pointer">
                            Excel файл
                            <input type="file" id="importExcelFile" accept=".xlsx,.xls" class="hidden" onchange="importFromExcel(event)">
                        </label>
                        <button onclick="showExcelTemplate()" class="block w-full text-left px-4 py-2 hover:bg-gray-100 text-sm">Создать из шаблона Excel</button>
                        <button onclick="downloadExcelTemplate()" class="block w-full text-left px-4 py-2 hover:bg-gray-100 text-sm">Скачать шаблон Excel</button>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-2 gap-4 mb-4">
                <div class="p-4 bg-gray-50 rounded-lg">
                    <h3 class="font-semibold mb-2">Легенда грейдов:</h3>
                    <div class="grid grid-cols-2 gap-2 text-sm">
                        <div class="flex items-center gap-2">
                            <div class="w-4 h-4 bg-red-500 rounded"></div>
                            <span>1-5: Стажеры/Младшие</span>
                        </div>
                        <div class="flex items-center gap-2">
                            <div class="w-4 h-4 bg-orange-500 rounded"></div>
                            <span>6-10: Специалисты</span>
                        </div>
                        <div class="flex items-center gap-2">
                            <div class="w-4 h-4 bg-yellow-500 rounded"></div>
                            <span>11-15: Старшие специалисты</span>
                        </div>
                        <div class="flex items-center gap-2">
                            <div class="w-4 h-4 bg-green-500 rounded"></div>
                            <span>16-20: Руководители групп</span>
                        </div>
                        <div class="flex items-center gap-2">
                            <div class="w-4 h-4 bg-blue-500 rounded"></div>
                            <span>21-25: Менеджеры</span>
                        </div>
                        <div class="flex items-center gap-2">
                            <div class="w-4 h-4 bg-purple-500 rounded"></div>
                            <span>26-30: Топ-менеджмент</span>
                        </div>
                    </div>
                </div>
                
                <div class="p-4 bg-blue-50 rounded-lg">
                    <h3 class="font-semibold mb-2">Статистика:</h3>
                    <div id="statistics" class="text-sm space-y-1">
                        <!-- Статистика будет отображаться здесь -->
                    </div>
                </div>
            </div>
        </div>

        <div id="orgChart" class="bg-white rounded-lg shadow-lg p-6 min-h-96 overflow-auto">
            <div id="treeContainer" class="org-tree">
                <!-- Дерево будет отображаться здесь -->
            </div>
        </div>

        <div id="gradeView" class="hidden bg-white rounded-lg shadow-lg p-6 mt-6">
            <h2 class="text-2xl font-bold text-gray-800 mb-4">Группировка по грейдам</h2>
            <div id="gradeGroups"></div>
        </div>
    </div>

    <!-- Модальное окно для добавления/редактирования -->
    <div id="modal" class="hidden fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
        <div class="bg-white rounded-lg p-6 w-96 max-w-90vw max-h-90vh overflow-y-auto">
            <h3 id="modalTitle" class="text-xl font-bold mb-4">Добавить подразделение</h3>
            <form id="departmentForm">
                <div class="mb-4">
                    <label class="block text-sm font-medium text-gray-700 mb-2 required-field">Название подразделения</label>
                    <input type="text" id="deptName" class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" required>
                </div>
                
                <div class="mb-4">
                    <label class="block text-sm font-medium text-gray-700 mb-2 required-field">Руководитель подразделения</label>
                    <input type="text" id="deptHead" class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Введите ФИО или должность руководителя" required>
                </div>
                
                <div class="mb-6">
                    <label class="block text-sm font-medium text-gray-700 mb-2">Описание подразделения</label>
                    <textarea id="deptDescription" class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" rows="2" placeholder="Краткое описание функций подразделения"></textarea>
                </div>

                <div class="mb-4">
                    <div class="flex justify-between items-center mb-3">
                        <h4 class="text-lg font-semibold text-gray-800">Позиции в подразделении</h4>
                        <button type="button" onclick="addPosition()" class="bg-green-500 hover:bg-green-600 text-white px-3 py-1 rounded text-sm transition-colors">
                            + Добавить позицию
                        </button>
                    </div>
                    <div id="positionsContainer" class="space-y-3 max-h-60 overflow-y-auto">
                        <!-- Позиции будут добавляться здесь -->
                    </div>
                </div>
                
                <div class="flex gap-3">
                    <button type="submit" class="flex-1 bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded-lg font-medium transition-colors">
                        Сохранить
                    </button>
                    <button type="button" onclick="closeModal()" class="flex-1 bg-gray-300 hover:bg-gray-400 text-gray-700 py-2 px-4 rounded-lg font-medium transition-colors">
                        Отмена
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Модальное окно Excel шаблона -->
    <div id="excelModal" class="hidden fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
        <div class="bg-white rounded-lg p-6 w-full max-w-6xl max-h-90vh overflow-y-auto mx-4">
            <div class="flex justify-between items-center mb-4">
                <h3 class="text-xl font-bold">Excel шаблон для импорта организационной структуры</h3>
                <button onclick="closeExcelModal()" class="text-gray-500 hover:text-gray-700 text-2xl">&times;</button>
            </div>
            
            <div class="mb-4 p-4 bg-blue-50 rounded-lg">
                <h4 class="font-semibold text-blue-800 mb-2">Инструкция по заполнению:</h4>
                <ul class="text-sm text-blue-700 space-y-1">
                    <li>• <strong>ID подразделения:</strong> Уникальный номер подразделения (например: 1, 2, 3...)</li>
                    <li>• <strong>ID родителя:</strong> Номер родительского подразделения (пустое для корневых подразделений)</li>
                    <li>• <strong>Название подразделения:</strong> Полное название подразделения</li>
                    <li>• <strong>Руководитель:</strong> ФИО или должность руководителя <span class="text-red-600 font-bold">(обязательно)</span></li>
                    <li>• <strong>Описание:</strong> Краткое описание функций подразделения (необязательно)</li>
                    <li>• <strong>Название позиции:</strong> Точное название должности</li>
                    <li>• <strong>Грейд:</strong> Число от 1 до 30</li>
                    <li>• <strong>Тип персонала:</strong> АУП или ПП</li>
                    <li>• <strong>Вид персонала:</strong> штат или аутстаффинг</li>
                    <li>• <strong>Количество:</strong> Число сотрудников на данной позиции</li>
                </ul>
            </div>
            
            <div class="table-container mb-4">
                <table id="excelTable" class="excel-table">
                    <thead>
                        <tr>
                            <th>ID подразделения</th>
                            <th>ID родителя</th>
                            <th>Название подразделения</th>
                            <th>Руководитель *</th>
                            <th>Описание</th>
                            <th>Название позиции</th>
                            <th>Грейд</th>
                            <th>Тип персонала</th>
                            <th>Вид персонала</th>
                            <th>Количество</th>
                        </tr>
                    </thead>
                    <tbody id="excelTableBody">
                        <!-- Строки будут добавляться динамически -->
                    </tbody>
                </table>
            </div>
            
            <div class="flex gap-3">
                <button onclick="addExcelRow()" class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg">
                    + Добавить строку
                </button>
                <button onclick="importFromExcelTable()" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg">
                    Импортировать данные
                </button>
                <button onclick="exportExcelTable()" class="bg-purple-600 hover:bg-purple-700 text-white px-4 py-2 rounded-lg">
                    Скачать как Excel
                </button>
                <button onclick="closeExcelModal()" class="bg-gray-300 hover:bg-gray-400 text-gray-700 px-4 py-2 rounded-lg">
                    Отмена
                </button>
            </div>
        </div>
    </div>

    <!-- Уведомления -->
    <div id="notifications" class="fixed top-4 right-4 space-y-2 z-50"></div>

    <script>
        let orgStructure = [];
        let currentEditId = null;
        let gradeViewActive = false;
        let excelData = [];

        function generateId() {
            return Date.now().toString(36) + Math.random().toString(36).substr(2);
        }

        function getGradeClass(grade) {
            if (grade <= 5) return 'grade-1-5';
            if (grade <= 10) return 'grade-6-10';
            if (grade <= 15) return 'grade-11-15';
            if (grade <= 20) return 'grade-16-20';
            if (grade <= 25) return 'grade-21-25';
            return 'grade-26-30';
        }

        function showNotification(message, type = 'success') {
            const notification = document.createElement('div');
            const bgColor = type === 'success' ? 'bg-green-500' : type === 'error' ? 'bg-red-500' : 'bg-blue-500';
            
            notification.className = `${bgColor} text-white px-6 py-3 rounded-lg shadow-lg transform transition-all duration-300 translate-x-full opacity-0`;
            notification.textContent = message;
            
            document.getElementById('notifications').appendChild(notification);
            
            setTimeout(() => {
                notification.classList.remove('translate-x-full', 'opacity-0');
            }, 100);
            
            setTimeout(() => {
                notification.classList.add('translate-x-full', 'opacity-0');
                setTimeout(() => {
                    if (notification.parentNode) {
                        notification.parentNode.removeChild(notification);
                    }
                }, 300);
            }, 3000);
        }

        // Закрытие меню при клике вне их
        document.addEventListener('click', function(e) {
            if (!e.target.closest('#exportMenu') && !e.target.closest('[onclick="toggleExportMenu()"]')) {
                document.getElementById('exportMenu').classList.add('hidden');
            }
            if (!e.target.closest('#importMenu') && !e.target.closest('[onclick="toggleImportMenu()"]')) {
                document.getElementById('importMenu').classList.add('hidden');
            }
        });

        function toggleExportMenu() {
            const menu = document.getElementById('exportMenu');
            menu.classList.toggle('hidden');
            document.getElementById('importMenu').classList.add('hidden');
        }

        function toggleImportMenu() {
            const menu = document.getElementById('importMenu');
            menu.classList.toggle('hidden');
            document.getElementById('exportMenu').classList.add('hidden');
        }

        // Экспорт в JSON
        function exportToJSON() {
            try {
                const data = JSON.stringify(orgStructure, null, 2);
                const blob = new Blob([data], { type: 'application/json' });
                const url = URL.createObjectURL(blob);
                const a = document.createElement('a');
                a.href = url;
                a.download = `org-structure-${new Date().toISOString().split('T')[0]}.json`;
                document.body.appendChild(a);
                a.click();
                document.body.removeChild(a);
                URL.revokeObjectURL(url);
                showNotification('Структура успешно экспортирована в JSON!');
                document.getElementById('exportMenu').classList.add('hidden');
            } catch (error) {
                showNotification('Ошибка при экспорте в JSON: ' + error.message, 'error');
            }
        }

        // Экспорт в Excel
        function exportToExcel() {
            try {
                const data = [];
                
                function flattenStructure(structure, parentId = '') {
                    structure.forEach(dept => {
                        if (dept.positions && dept.positions.length > 0) {
                            dept.positions.forEach(pos => {
                                data.push({
                                    'ID подразделения': dept.id,
                                    'ID родителя': parentId,
                                    'Название подразделения': dept.name,
                                    'Руководитель': dept.head || '',
                                    'Описание': dept.description || '',
                                    'Название позиции': pos.title,
                                    'Грейд': pos.grade,
                                    'Тип персонала': pos.personnelType,
                                    'Вид персонала': pos.personnelKind,
                                    'Количество': pos.staffCount
                                });
                            });
                        } else {
                            data.push({
                                'ID подразделения': dept.id,
                                'ID родителя': parentId,
                                'Название подразделения': dept.name,
                                'Руководитель': dept.head || '',
                                'Описание': dept.description || '',
                                'Название позиции': '',
                                'Грейд': '',
                                'Тип персонала': '',
                                'Вид персонала': '',
                                'Количество': ''
                            });
                        }
                        
                        if (dept.children && dept.children.length > 0) {
                            flattenStructure(dept.children, dept.id);
                        }
                    });
                }
                
                flattenStructure(orgStructure);
                
                const ws = XLSX.utils.json_to_sheet(data);
                const wb = XLSX.utils.book_new();
                XLSX.utils.book_append_sheet(wb, ws, "Организационная структура");
                
                XLSX.writeFile(wb, `org-structure-${new Date().toISOString().split('T')[0]}.xlsx`);
                showNotification('Структура успешно экспортирована в Excel!');
                document.getElementById('exportMenu').classList.add('hidden');
            } catch (error) {
                showNotification('Ошибка при экспорте в Excel: ' + error.message, 'error');
            }
        }

        // Импорт из JSON
        function importFromJSON(event) {
            const file = event.target.files[0];
            if (!file) return;

            const reader = new FileReader();
            reader.onload = function(e) {
                try {
                    const importedData = JSON.parse(e.target.result);
                    
                    if (Array.isArray(importedData)) {
                        if (confirm('Импорт заменит текущую структуру. Продолжить?')) {
                            orgStructure = importedData;
                            renderTree();
                            showNotification('Структура успешно импортирована из JSON!');
                        }
                    } else {
                        showNotification('Неверный формат JSON файла. Ожидается массив подразделений.', 'error');
                    }
                } catch (error) {
                    showNotification('Ошибка при чтении JSON файла: ' + error.message, 'error');
                }
            };
            reader.readAsText(file);
            
            event.target.value = '';
            document.getElementById('importMenu').classList.add('hidden');
        }

        // Импорт из Excel
        function importFromExcel(event) {
            const file = event.target.files[0];
            if (!file) return;

            const reader = new FileReader();
            reader.onload = function(e) {
                try {
                    const data = new Uint8Array(e.target.result);
                    const workbook = XLSX.read(data, {type: 'array'});
                    const firstSheetName = workbook.SheetNames[0];
                    const worksheet = workbook.Sheets[firstSheetName];
                    const jsonData = XLSX.utils.sheet_to_json(worksheet);
                    
                    if (jsonData.length === 0) {
                        showNotification('Excel файл пуст или не содержит данных', 'error');
                        return;
                    }
                    
                    if (confirm('Импорт заменит текущую структуру. Продолжить?')) {
                        const newStructure = buildStructureFromExcel(jsonData);
                        if (newStructure) {
                            orgStructure = newStructure;
                            renderTree();
                            showNotification('Структура успешно импортирована из Excel!');
                        }
                    }
                } catch (error) {
                    showNotification('Ошибка при чтении Excel файла: ' + error.message, 'error');
                }
            };
            reader.readAsArrayBuffer(file);
            
            event.target.value = '';
            document.getElementById('importMenu').classList.add('hidden');
        }

        function buildStructureFromExcel(data) {
            try {
                const departments = {};
                const departmentData = {};
                
                // Группируем данные по подразделениям
                data.forEach(row => {
                    const deptId = String(row['ID подразделения'] || '').trim();
                    const parentId = String(row['ID родителя'] || '').trim();
                    const deptName = String(row['Название подразделения'] || '').trim();
                    const head = String(row['Руководитель'] || '').trim();
                    const description = String(row['Описание'] || '').trim();
                    const positionTitle = String(row['Название позиции'] || '').trim();
                    const grade = parseInt(row['Грейд']) || 1;
                    const personnelType = String(row['Тип персонала'] || 'АУП').trim();
                    const personnelKind = String(row['Вид персонала'] || 'штат').trim();
                    const staffCount = parseInt(row['Количество']) || 1;
                    
                    if (!deptId || !deptName || !head) {
                        showNotification('Пропущена строка: не заполнены обязательные поля (ID подразделения, Название, Руководитель)', 'error');
                        return;
                    }
                    
                    if (!departmentData[deptId]) {
                        departmentData[deptId] = {
                            id: generateId(),
                            originalId: deptId,
                            parentId: parentId,
                            name: deptName,
                            head: head,
                            description: description,
                            positions: [],
                            children: []
                        };
                    }
                    
                    if (positionTitle) {
                        departmentData[deptId].positions.push({
                            id: generateId(),
                            title: positionTitle,
                            grade: grade,
                            personnelType: personnelType,
                            personnelKind: personnelKind,
                            staffCount: staffCount
                        });
                    }
                });
                
                // Строим иерархию
                const rootDepartments = [];
                const allDepartments = Object.values(departmentData);
                
                allDepartments.forEach(dept => {
                    dept.collapsed = false;
                    if (dept.parentId) {
                        const parent = allDepartments.find(d => d.originalId === dept.parentId);
                        if (parent) {
                            parent.children.push(dept);
                        } else {
                            rootDepartments.push(dept);
                        }
                    } else {
                        rootDepartments.push(dept);
                    }
                });
                
                // Удаляем временное поле originalId
                function cleanIds(structure) {
                    structure.forEach(dept => {
                        delete dept.originalId;
                        if (dept.children && dept.children.length > 0) {
                            cleanIds(dept.children);
                        }
                    });
                }
                
                cleanIds(rootDepartments);
                return rootDepartments;
                
            } catch (error) {
                showNotification('Ошибка при обработке данных Excel: ' + error.message, 'error');
                return null;
            }
        }

        // Показать Excel шаблон
        function showExcelTemplate() {
            excelData = [
                {
                    deptId: '1',
                    parentId: '',
                    deptName: 'Генеральная дирекция',
                    head: 'Генеральный директор',
                    description: 'Высший орган управления компанией',
                    positionTitle: 'Генеральный директор',
                    grade: '30',
                    personnelType: 'АУП',
                    personnelKind: 'штат',
                    staffCount: '1'
                },
                {
                    deptId: '2',
                    parentId: '1',
                    deptName: 'Финансовый департамент',
                    head: 'Финансовый директор',
                    description: 'Управление финансами и бюджетированием',
                    positionTitle: 'Финансовый директор',
                    grade: '25',
                    personnelType: 'АУП',
                    personnelKind: 'штат',
                    staffCount: '1'
                },
                {
                    deptId: '2',
                    parentId: '1',
                    deptName: 'Финансовый департамент',
                    head: 'Финансовый директор',
                    description: '',
                    positionTitle: 'Главный бухгалтер',
                    grade: '20',
                    personnelType: 'АУП',
                    personnelKind: 'штат',
                    staffCount: '1'
                },
                {
                    deptId: '3',
                    parentId: '1',
                    deptName: 'IT департамент',
                    head: 'IT директор',
                    description: 'Информационные технологии и разработка',
                    positionTitle: 'IT директор',
                    grade: '25',
                    personnelType: 'АУП',
                    personnelKind: 'штат',
                    staffCount: '1'
                },
                {
                    deptId: '3',
                    parentId: '1',
                    deptName: 'IT департамент',
                    head: 'IT директор',
                    description: '',
                    positionTitle: 'Ведущий разработчик',
                    grade: '18',
                    personnelType: 'ПП',
                    personnelKind: 'штат',
                    staffCount: '2'
                }
            ];
            
            document.getElementById('excelModal').classList.remove('hidden');
            renderExcelTable();
            document.getElementById('importMenu').classList.add('hidden');
        }

        function renderExcelTable() {
            const tbody = document.getElementById('excelTableBody');
            tbody.innerHTML = '';
            
            excelData.forEach((row, index) => {
                const tr = document.createElement('tr');
                tr.innerHTML = `
                    <td><input type="text" value="${row.deptId}" onchange="updateExcelData(${index}, 'deptId', this.value)"></td>
                    <td><input type="text" value="${row.parentId}" onchange="updateExcelData(${index}, 'parentId', this.value)"></td>
                    <td><input type="text" value="${row.deptName}" onchange="updateExcelData(${index}, 'deptName', this.value)"></td>
                    <td><input type="text" value="${row.head}" onchange="updateExcelData(${index}, 'head', this.value)" required placeholder="Обязательное поле"></td>
                    <td><input type="text" value="${row.description}" onchange="updateExcelData(${index}, 'description', this.value)"></td>
                    <td><input type="text" value="${row.positionTitle}" onchange="updateExcelData(${index}, 'positionTitle', this.value)"></td>
                    <td><input type="number" min="1" max="30" value="${row.grade}" onchange="updateExcelData(${index}, 'grade', this.value)"></td>
                    <td>
                        <select onchange="updateExcelData(${index}, 'personnelType', this.value)">
                            <option value="АУП" ${row.personnelType === 'АУП' ? 'selected' : ''}>АУП</option>
                            <option value="ПП" ${row.personnelType === 'ПП' ? 'selected' : ''}>ПП</option>
                        </select>
                    </td>
                    <td>
                        <select onchange="updateExcelData(${index}, 'personnelKind', this.value)">
                            <option value="штат" ${row.personnelKind === 'штат' ? 'selected' : ''}>штат</option>
                            <option value="аутстаффинг" ${row.personnelKind === 'аутстаффинг' ? 'selected' : ''}>аутстаффинг</option>
                        </select>
                    </td>
                    <td><input type="number" min="1" value="${row.staffCount}" onchange="updateExcelData(${index}, 'staffCount', this.value)"></td>
                `;
                tbody.appendChild(tr);
            });
        }

        function updateExcelData(index, field, value) {
            if (excelData[index]) {
                excelData[index][field] = value;
            }
        }

        function addExcelRow() {
            excelData.push({
                deptId: '',
                parentId: '',
                deptName: '',
                head: '',
                description: '',
                positionTitle: '',
                grade: '1',
                personnelType: 'АУП',
                personnelKind: 'штат',
                staffCount: '1'
            });
            renderExcelTable();
        }

        function importFromExcelTable() {
            try {
                if (excelData.length === 0) {
                    showNotification('Нет данных для импорта', 'error');
                    return;
                }
                
                // Проверяем обязательные поля
                const invalidRows = excelData.filter(row => !row.deptId || !row.deptName || !row.head);
                if (invalidRows.length > 0) {
                    showNotification('Заполните все обязательные поля: ID подразделения, Название подразделения, Руководитель', 'error');
                    return;
                }
                
                // Преобразуем данные в правильный формат
                const convertedData = excelData.map(row => ({
                    'ID подразделения': row.deptId,
                    'ID родителя': row.parentId,
                    'Название подразделения': row.deptName,
                    'Руководитель': row.head,
                    'Описание': row.description,
                    'Название позиции': row.positionTitle,
                    'Грейд': parseInt(row.grade) || 1,
                    'Тип персонала': row.personnelType,
                    'Вид персонала': row.personnelKind,
                    'Количество': parseInt(row.staffCount) || 1
                }));
                
                if (confirm('Импорт заменит текущую структуру. Продолжить?')) {
                    const newStructure = buildStructureFromExcel(convertedData);
                    if (newStructure) {
                        orgStructure = newStructure;
                        renderTree();
                        closeExcelModal();
                        showNotification('Структура успешно импортирована из таблицы!');
                    }
                }
            } catch (error) {
                showNotification('Ошибка при импорте из таблицы: ' + error.message, 'error');
            }
        }

        function exportExcelTable() {
            try {
                const data = excelData.map(row => ({
                    'ID подразделения': row.deptId,
                    'ID родителя': row.parentId,
                    'Название подразделения': row.deptName,
                    'Руководитель': row.head,
                    'Описание': row.description,
                    'Название позиции': row.positionTitle,
                    'Грейд': row.grade,
                    'Тип персонала': row.personnelType,
                    'Вид персонала': row.personnelKind,
                    'Количество': row.staffCount
                }));
                
                const ws = XLSX.utils.json_to_sheet(data);
                const wb = XLSX.utils.book_new();
                XLSX.utils.book_append_sheet(wb, ws, "Шаблон структуры");
                
                XLSX.writeFile(wb, `template-org-structure-${new Date().toISOString().split('T')[0]}.xlsx`);
                showNotification('Шаблон Excel успешно скачан!');
            } catch (error) {
                showNotification('Ошибка при экспорте шаблона Excel: ' + error.message, 'error');
            }
        }

        function downloadExcelTemplate() {
            try {
                const templateData = [
                    {
                        'ID подразделения': '1',
                        'ID родителя': '',
                        'Название подразделения': 'Генеральная дирекция',
                        'Руководитель': 'Генеральный директор',
                        'Описание': 'Высший орган управления компанией',
                        'Название позиции': 'Генеральный директор',
                        'Грейд': 30,
                        'Тип персонала': 'АУП',
                        'Вид персонала': 'штат',
                        'Количество': 1
                    },
                    {
                        'ID подразделения': '2',
                        'ID родителя': '1',
                        'Название подразделения': 'Финансовый департамент',
                        'Руководитель': 'Финансовый директор',
                        'Описание': 'Управление финансами и бюджетированием',
                        'Название позиции': 'Финансовый директор',
                        'Грейд': 25,
                        'Тип персонала': 'АУП',
                        'Вид персонала': 'штат',
                        'Количество': 1
                    },
                    {
                        'ID подразделения': '2',
                        'ID родителя': '1',
                        'Название подразделения': 'Финансовый департамент',
                        'Руководитель': 'Финансовый директор',
                        'Описание': '',
                        'Название позиции': 'Главный бухгалтер',
                        'Грейд': 20,
                        'Тип персонала': 'АУП',
                        'Вид персонала': 'штат',
                        'Количество': 1
                    },
                    {
                        'ID подразделения': '3',
                        'ID родителя': '1',
                        'Название подразделения': 'IT департамент',
                        'Руководитель': 'IT директор',
                        'Описание': 'Информационные технологии и разработка',
                        'Название позиции': 'IT директор',
                        'Грейд': 25,
                        'Тип персонала': 'АУП',
                        'Вид персонала': 'штат',
                        'Количество': 1
                    },
                    {
                        'ID подразделения': '3',
                        'ID родителя': '1',
                        'Название подразделения': 'IT департамент',
                        'Руководитель': 'IT директор',
                        'Описание': '',
                        'Название позиции': 'Ведущий разработчик',
                        'Грейд': 18,
                        'Тип персонала': 'ПП',
                        'Вид персонала': 'штат',
                        'Количество': 2
                    }
                ];
                
                const ws = XLSX.utils.json_to_sheet(templateData);
                const wb = XLSX.utils.book_new();
                XLSX.utils.book_append_sheet(wb, ws, "Шаблон");
                
                XLSX.writeFile(wb, `template-org-structure.xlsx`);
                showNotification('Шаблон Excel успешно скачан!');
                document.getElementById('importMenu').classList.add('hidden');
            } catch (error) {
                showNotification('Ошибка при скачивании шаблона: ' + error.message, 'error');
            }
        }

        function closeExcelModal() {
            document.getElementById('excelModal').classList.add('hidden');
        }

        function addRootDepartment() {
            currentEditId = null;
            currentPositions = [];
            document.getElementById('modalTitle').textContent = 'Добавить корневое подразделение';
            document.getElementById('departmentForm').reset();
            renderPositions();
            document.getElementById('modal').classList.remove('hidden');
        }

        function addChildDepartment(parentId) {
            currentEditId = null;
            currentPositions = [];
            document.getElementById('modalTitle').textContent = 'Добавить подразделение';
            document.getElementById('departmentForm').reset();
            renderPositions();
            document.getElementById('modal').classList.remove('hidden');
            document.getElementById('modal').dataset.parentId = parentId;
        }

        function editDepartment(id) {
            const dept = findDepartmentById(id, orgStructure);
            if (dept) {
                currentEditId = id;
                document.getElementById('modalTitle').textContent = 'Редактировать подразделение';
                document.getElementById('deptName').value = dept.name;
                document.getElementById('deptHead').value = dept.head || '';
                document.getElementById('deptDescription').value = dept.description || '';
                
                currentPositions = dept.positions ? [...dept.positions] : [];
                renderPositions();
                
                document.getElementById('modal').classList.remove('hidden');
            }
        }

        function deleteDepartment(id) {
            if (confirm('Вы уверены, что хотите удалить это подразделение и все его подразделения?')) {
                orgStructure = removeDepartmentById(id, orgStructure);
                renderTree();
                showNotification('Подразделение успешно удалено');
            }
        }

        function findDepartmentById(id, structure) {
            for (let dept of structure) {
                if (dept.id === id) return dept;
                if (dept.children) {
                    const found = findDepartmentById(id, dept.children);
                    if (found) return found;
                }
            }
            return null;
        }

        function removeDepartmentById(id, structure) {
            return structure.filter(dept => {
                if (dept.id === id) return false;
                if (dept.children) {
                    dept.children = removeDepartmentById(id, dept.children);
                }
                return true;
            });
        }

        function closeModal() {
            document.getElementById('modal').classList.add('hidden');
            document.getElementById('modal').removeAttribute('data-parent-id');
        }

        let currentPositions = [];

        function addPosition() {
            const positionId = generateId();
            const position = {
                id: positionId,
                title: '',
                grade: 1,
                personnelType: 'АУП',
                personnelKind: 'штат',
                staffCount: 1
            };
            currentPositions.push(position);
            renderPositions();
        }

        function removePosition(positionId) {
            currentPositions = currentPositions.filter(p => p.id !== positionId);
            renderPositions();
        }

        function renderPositions() {
            const container = document.getElementById('positionsContainer');
            if (currentPositions.length === 0) {
                container.innerHTML = '<div class="text-gray-500 text-sm text-center py-4">Нажмите "Добавить позицию" для создания первой позиции</div>';
                return;
            }

            container.innerHTML = currentPositions.map(position => `
                <div class="border border-gray-200 rounded-lg p-3 bg-gray-50">
                    <div class="flex justify-between items-start mb-3">
                        <input type="text" placeholder="Название позиции" value="${position.title}" 
                               onchange="updatePosition('${position.id}', 'title', this.value)"
                               class="flex-1 border border-gray-300 rounded px-2 py-1 text-sm mr-2" required>
                        <button type="button" onclick="removePosition('${position.id}')" 
                                class="bg-red-500 hover:bg-red-600 text-white px-2 py-1 rounded text-xs">
                            ✕
                        </button>
                    </div>
                    <div class="grid grid-cols-2 gap-2">
                        <div>
                            <label class="block text-xs text-gray-600 mb-1">Грейд</label>
                            <input type="number" min="1" max="30" value="${position.grade}"
                                   onchange="updatePosition('${position.id}', 'grade', parseInt(this.value))"
                                   class="w-full border border-gray-300 rounded px-2 py-1 text-sm">
                        </div>
                        <div>
                            <label class="block text-xs text-gray-600 mb-1">Количество</label>
                            <input type="number" min="1" value="${position.staffCount}"
                                   onchange="updatePosition('${position.id}', 'staffCount', parseInt(this.value))"
                                   class="w-full border border-gray-300 rounded px-2 py-1 text-sm">
                        </div>
                        <div>
                            <label class="block text-xs text-gray-600 mb-1">Тип</label>
                            <select onchange="updatePosition('${position.id}', 'personnelType', this.value)"
                                    class="w-full border border-gray-300 rounded px-2 py-1 text-sm">
                                <option value="АУП" ${position.personnelType === 'АУП' ? 'selected' : ''}>АУП</option>
                                <option value="ПП" ${position.personnelType === 'ПП' ? 'selected' : ''}>ПП</option>
                            </select>
                        </div>
                        <div>
                            <label class="block text-xs text-gray-600 mb-1">Вид</label>
                            <select onchange="updatePosition('${position.id}', 'personnelKind', this.value)"
                                    class="w-full border border-gray-300 rounded px-2 py-1 text-sm">
                                <option value="штат" ${position.personnelKind === 'штат' ? 'selected' : ''}>Штат</option>
                                <option value="аутстаффинг" ${position.personnelKind === 'аутстаффинг' ? 'selected' : ''}>Аутстаффинг</option>
                            </select>
                        </div>
                    </div>
                </div>
            `).join('');
        }

        function updatePosition(positionId, field, value) {
            const position = currentPositions.find(p => p.id === positionId);
            if (position) {
                position[field] = value;
            }
        }

        document.getElementById('departmentForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const name = document.getElementById('deptName').value.trim();
            const head = document.getElementById('deptHead').value.trim();
            const description = document.getElementById('deptDescription').value.trim();

            if (!name) {
                showNotification('Введите название подразделения', 'error');
                return;
            }

            if (!head) {
                showNotification('Введите руководителя подразделения', 'error');
                return;
            }

            if (currentPositions.length === 0) {
                showNotification('Добавьте хотя бы одну позицию в подразделение', 'error');
                return;
            }

            const emptyPositions = currentPositions.filter(p => !p.title.trim());
            if (emptyPositions.length > 0) {
                showNotification('Заполните названия всех позиций', 'error');
                return;
            }

            const department = {
                id: currentEditId || generateId(),
                name,
                head,
                description,
                positions: [...currentPositions],
                children: [],
                collapsed: false
            };

            if (currentEditId) {
                updateDepartment(currentEditId, department);
                showNotification('Подразделение успешно обновлено');
            } else {
                const parentId = document.getElementById('modal').dataset.parentId;
                if (parentId) {
                    addToParent(parentId, department);
                } else {
                    orgStructure.push(department);
                }
                showNotification('Подразделение успешно добавлено');
            }

            renderTree();
            closeModal();
        });

        function updateDepartment(id, newData) {
            function update(structure) {
                for (let dept of structure) {
                    if (dept.id === id) {
                        dept.name = newData.name;
                        dept.head = newData.head;
                        dept.description = newData.description;
                        dept.positions = newData.positions;
                        return true;
                    }
                    if (dept.children && update(dept.children)) {
                        return true;
                    }
                }
                return false;
            }
            update(orgStructure);
        }

        function addToParent(parentId, department) {
            function findAndAdd(structure) {
                for (let dept of structure) {
                    if (dept.id === parentId) {
                        if (!dept.children) dept.children = [];
                        dept.children.push(department);
                        return true;
                    }
                    if (dept.children && findAndAdd(dept.children)) {
                        return true;
                    }
                }
                return false;
            }
            findAndAdd(orgStructure);
        }

        function toggleCollapse(id) {
            function toggle(structure) {
                for (let dept of structure) {
                    if (dept.id === id) {
                        dept.collapsed = !dept.collapsed;
                        return true;
                    }
                    if (dept.children && toggle(dept.children)) {
                        return true;
                    }
                }
                return false;
            }
            toggle(orgStructure);
            renderTree();
        }

        function renderTree() {
            const container = document.getElementById('treeContainer');
            container.innerHTML = '';
            
            if (orgStructure.length === 0) {
                container.innerHTML = '<div class="text-center text-gray-500 py-8">Нажмите "Добавить корневое подразделение" для начала</div>';
                return;
            }

            // Создаем первый уровень - корневые подразделения
            renderLevel(orgStructure, container);
            updateStatistics();
        }

        function renderLevel(departments, parentContainer) {
            if (!departments || departments.length === 0) return;

            const levelDiv = document.createElement('div');
            levelDiv.className = 'org-level';

            departments.forEach(dept => {
                const deptDiv = document.createElement('div');
                deptDiv.className = 'org-department';
                
                // Добавляем класс has-children если есть дети
                if (dept.children && dept.children.length > 0) {
                    deptDiv.classList.add('has-children');
                }
                
                // Создаем узел подразделения
                const nodeDiv = renderDepartmentNode(dept);
                deptDiv.appendChild(nodeDiv);
                
                // Добавляем детей, если они есть и подразделение не свернуто
                if (dept.children && dept.children.length > 0 && !dept.collapsed) {
                    const childrenContainer = document.createElement('div');
                    childrenContainer.className = 'org-children';
                    
                    renderLevel(dept.children, childrenContainer);
                    deptDiv.appendChild(childrenContainer);
                }
                
                levelDiv.appendChild(deptDiv);
            });

            parentContainer.appendChild(levelDiv);
        }

        function renderDepartmentNode(dept) {
            const nodeDiv = document.createElement('div');
            nodeDiv.className = 'org-node';
            
            const totalStaff = dept.positions ? dept.positions.reduce((sum, pos) => sum + pos.staffCount, 0) : 0;
            const gradeRange = dept.positions && dept.positions.length > 0 ? 
                `${Math.min(...dept.positions.map(p => p.grade))}-${Math.max(...dept.positions.map(p => p.grade))}` : '';
            const hasChildren = dept.children && dept.children.length > 0;
            const collapseIcon = dept.children && dept.children.length > 0 ? (dept.collapsed ? "▶" : "▼") : "";
            const minGrade = dept.positions && dept.positions.length > 0 ? Math.min(...dept.positions.map(p => p.grade)) : 1;

            nodeDiv.innerHTML = `
                <div class="bg-white border-2 border-gray-200 rounded-lg p-4 shadow-md hover:shadow-lg transition-all duration-200 ${getGradeClass(minGrade)}">
                    <div class="flex justify-between items-start mb-2">
                        <div class="flex-1">
                            <div class="flex items-center gap-2">
                                ${hasChildren ? `<button onclick="toggleCollapse('${dept.id}')" class="text-gray-500 hover:text-gray-700 font-bold text-sm w-5 h-5 flex items-center justify-center transition-colors">${collapseIcon}</button>` : '<div class="w-5"></div>'}
                                <h3 class="font-bold text-lg text-gray-800 leading-tight">${dept.name}</h3>
                            </div>
                            <p class="text-sm text-gray-600 mt-1 ml-7 font-medium">👤 ${dept.head}</p>
                        </div>
                        <div class="flex gap-1 ml-2 opacity-80 hover:opacity-100 transition-opacity">
                            <button onclick="addChildDepartment('${dept.id}')" class="bg-green-500 hover:bg-green-600 text-white p-1.5 rounded text-xs transition-colors" title="Добавить подразделение">
                                ➕
                            </button>
                            <button onclick="editDepartment('${dept.id}')" class="bg-blue-500 hover:bg-blue-600 text-white p-1.5 rounded text-xs transition-colors" title="Редактировать">
                                ✏️
                            </button>
                            <button onclick="deleteDepartment('${dept.id}')" class="bg-red-500 hover:bg-red-600 text-white p-1.5 rounded text-xs transition-colors" title="Удалить">
                                🗑️
                            </button>
                        </div>
                    </div>
                    
                    <div class="flex flex-wrap items-center gap-2 mb-3 ml-7">
                        ${gradeRange ? `<span class="bg-blue-100 text-blue-800 text-xs font-semibold px-2 py-1 rounded-full">Грейды ${gradeRange}</span>` : ''}
                        <span class="bg-orange-100 text-orange-800 text-xs font-semibold px-2 py-1 rounded-full">
                            👥 ${totalStaff} чел.
                        </span>
                        <span class="bg-gray-100 text-gray-800 text-xs font-semibold px-2 py-1 rounded-full">
                            📋 ${dept.positions ? dept.positions.length : 0} поз.
                        </span>
                    </div>
                    
                    ${dept.description ? `<p class="text-sm text-gray-600 mb-3 ml-7 italic">${dept.description}</p>` : ''}
                    
                    ${dept.positions && dept.positions.length > 0 ? `
                        <div class="ml-7 bg-gray-50 rounded-lg p-3">
                            <div class="text-xs font-semibold text-gray-700 mb-2">Позиции:</div>
                            <div class="space-y-2">
                                ${dept.positions.map(pos => `
                                    <div class="flex items-center gap-2 text-xs flex-wrap bg-white rounded px-2 py-1">
                                        <span class="font-medium text-gray-800">${pos.title}</span>
                                        <span class="bg-blue-100 text-blue-700 px-2 py-0.5 rounded-full">Гр.${pos.grade}</span>
                                        <span class="bg-green-100 text-green-700 px-2 py-0.5 rounded-full">${pos.personnelType}</span>
                                        <span class="bg-purple-100 text-purple-700 px-2 py-0.5 rounded-full">${pos.personnelKind}</span>
                                        <span class="bg-orange-100 text-orange-700 px-2 py-0.5 rounded-full">${pos.staffCount} чел.</span>
                                    </div>
                                `).join('')}
                            </div>
                        </div>
                    ` : ''}
                </div>
            `;
            
            return nodeDiv;
        }

        function toggleGradeView() {
            gradeViewActive = !gradeViewActive;
            const gradeView = document.getElementById('gradeView');
            const treeView = document.getElementById('orgChart');
            
            if (gradeViewActive) {
                gradeView.classList.remove('hidden');
                treeView.classList.add('hidden');
                renderGradeView();
            } else {
                gradeView.classList.add('hidden');
                treeView.classList.remove('hidden');
            }
        }

        function renderGradeView() {
            const container = document.getElementById('gradeGroups');
            container.innerHTML = '';

            const gradeGroups = {};
            
            function collectPositions(structure) {
                structure.forEach(dept => {
                    if (dept.positions) {
                        dept.positions.forEach(pos => {
                            const gradeRange = getGradeRange(pos.grade);
                            if (!gradeGroups[gradeRange]) {
                                gradeGroups[gradeRange] = [];
                            }
                            gradeGroups[gradeRange].push({
                                ...pos,
                                departmentName: dept.name
                            });
                        });
                    }
                    
                    if (dept.children) {
                        collectPositions(dept.children);
                    }
                });
            }

            collectPositions(orgStructure);

            const ranges = ['1-5', '6-10', '11-15', '16-20', '21-25', '26-30'];
            const titles = ['Стажеры/Младшие', 'Специалисты', 'Старшие специалисты', 'Руководители групп', 'Менеджеры', 'Топ-менеджмент'];
            
            ranges.forEach((range, index) => {
                if (gradeGroups[range] && gradeGroups[range].length > 0) {
                    const groupDiv = document.createElement('div');
                    groupDiv.className = 'mb-6';
                    
                    groupDiv.innerHTML = `
                        <h3 class="text-xl font-bold mb-3 text-gray-800">
                            ${titles[index]} (Грейды ${range})
                        </h3>
                        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                            ${gradeGroups[range].map(pos => `
                                <div class="bg-white border-2 border-gray-200 rounded-lg p-4 ${getGradeClass(pos.grade)}">
                                    <h4 class="font-semibold text-gray-800">${pos.title}</h4>
                                    <p class="text-sm text-gray-600">📍 ${pos.departmentName}</p>
                                    <div class="flex flex-wrap gap-1 mt-2">
                                        <span class="bg-blue-100 text-blue-800 text-xs font-semibold px-2 py-1 rounded">
                                            Грейд ${pos.grade}
                                        </span>
                                        <span class="bg-green-100 text-green-800 text-xs font-semibold px-2 py-1 rounded">
                                            ${pos.personnelType}
                                        </span>
                                        <span class="bg-purple-100 text-purple-800 text-xs font-semibold px-2 py-1 rounded">
                                            ${pos.personnelKind}
                                        </span>
                                        <span class="bg-orange-100 text-orange-800 text-xs font-semibold px-2 py-1 rounded">
                                            👥 ${pos.staffCount} чел.
                                        </span>
                                    </div>
                                </div>
                            `).join('')}
                        </div>
                    `;
                    
                    container.appendChild(groupDiv);
                }
            });
        }

        function getGradeRange(grade) {
            if (grade <= 5) return '1-5';
            if (grade <= 10) return '6-10';
            if (grade <= 15) return '11-15';
            if (grade <= 20) return '16-20';
            if (grade <= 25) return '21-25';
            return '26-30';
        }

        function updateStatistics() {
            const stats = {
                totalDepartments: 0,
                totalPositions: 0,
                totalStaff: 0,
                gradeStats: {},
                personnelTypeStats: { 'АУП': 0, 'ПП': 0 },
                personnelKindStats: { 'штат': 0, 'аутстаффинг': 0 }
            };

            function collectStats(structure) {
                structure.forEach(dept => {
                    stats.totalDepartments++;
                    
                    if (dept.positions) {
                        dept.positions.forEach(pos => {
                            stats.totalPositions++;
                            stats.totalStaff += pos.staffCount;
                            
                            const gradeRange = getGradeRange(pos.grade);
                            stats.gradeStats[gradeRange] = (stats.gradeStats[gradeRange] || 0) + pos.staffCount;
                            
                            stats.personnelTypeStats[pos.personnelType] += pos.staffCount;
                            stats.personnelKindStats[pos.personnelKind] += pos.staffCount;
                        });
                    }
                    
                    if (dept.children) {
                        collectStats(dept.children);
                    }
                });
            }

            collectStats(orgStructure);

            const statsContainer = document.getElementById('statistics');
            statsContainer.innerHTML = `
                <div class="font-semibold text-blue-800">Общая статистика:</div>
                <div>📊 Всего подразделений: ${stats.totalDepartments}</div>
                <div>📋 Всего позиций: ${stats.totalPositions}</div>
                <div>👥 Общая численность: ${stats.totalStaff}</div>
                
                <div class="font-semibold text-blue-800 mt-2">По грейдам:</div>
                ${Object.entries(stats.gradeStats).map(([range, count]) => 
                    `<div>• ${range}: ${count} чел.</div>`
                ).join('')}
                
                <div class="font-semibold text-blue-800 mt-2">По типу персонала:</div>
                <div>• АУП: ${stats.personnelTypeStats['АУП']} чел.</div>
                <div>• ПП: ${stats.personnelTypeStats['ПП']} чел.</div>
                
                <div class="font-semibold text-blue-800 mt-2">По виду персонала:</div>
                <div>• Штат: ${stats.personnelKindStats['штат']} чел.</div>
                <div>• Аутстаффинг: ${stats.personnelKindStats['аутстаффинг']} чел.</div>
            `;
        }

        // Инициализация приложения
        renderTree();
        updateStatistics();
    </script>
</body>
</html>
